#include "common.h"
#include <vector>

void simulateStep(const std::vector<Node>& nodes,
                  std::vector<Node>& new_nodes){
    return;
}

int main(int argc, char *argv[]){
    StartupOptions options = parseOptions(argc, argv);
    return 0;
}